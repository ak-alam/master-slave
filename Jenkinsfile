pipeline {
    agent {
        label 'akbar-agent'
    }
    stages {
        stage('testing 123') {
            steps {
                sh 'echo hello from master node'
            }
        }
    }
}